REACT-1수업

3월11일

Git의 학습 방법
[초급을 환영합니다.]

git 초기화
git add .
git commit -m "설명"
git 원격 저장소 추가 출처 URL
[고급]

Pull Request(PR), 충돌 해결, Git 기록 정리 등의 학습이 목표
이 단계에서는 " Git 히스토리를 정리하는 법과 PR을 활용하여 협력 "이 중요합니다.
[고급을 약속합니다]

git 풀 리퀘스트
git fetch-pick
git reset --soft HEAD~1
git revert
git reflog
.gitignore
Git 버전 관리에서 제외할 파일 확장 목록을 정의하는 설정 파일
.gitignore 파일 생성 후 필요한 파일을 입력합니다.
노드.js
라이언은 달 2009년 Node.js를 발표했습니다.

배터리 블로킹 방식으로 처리가 가능합니다.

javascript 풀스택 개발 가능

독립체인: npm을 통해 다양한 패키지 사용 가능

야외 개발에 적합

행렬 데이터를 처리하는 방법

React 이전과 이후의 개발 방식의 차이

*이전

HTML과 CSS를 이용하여 구성을 구성합니다
JavaScript로 DOM을 직접 처리했습니다.
특정사건 발생 -> 필요한 부분만 수정
화면이 확장되는 확장과 DOM 추적이 어렵습니다.
이후(React가 나타난 이후)
화면을 직접 공격하지 않고 상태를 보호하면, 화면은 다시 압축됨 -> DOM을 핸들링하지 않고 전송을 함락함
React에서 전송이란 "현재 상태(state) 기준으로 UI를 다시 작업하는 과정을
개발자는 DOM을 어떻게 고칠 생각하지 않고, 상태가 무엇인지 정의하기 어렵습니다.
공식 문서에서 소개하는 React
React를 잘 사용하는 것은 상태와 관계를 맺을 수 있게 설명할 수 있고, 사용할 수 있는 것을
왜 이 상태(state)가 여기 있어야 하는지 설명할 수 있어야 합니다
왜 이 구성요소가 다시 연결되어야 하는지 설명할 수 있어야 합니다.
왜 효과가 필요하거나 필요하지 않은 설명할 수 있어야 함
React를 잘 쓰는 것의 의미는 무엇입니까?
UI를 구성하는 단위로 개발하여 레고를 조립해 완벽하게 완성하세요
